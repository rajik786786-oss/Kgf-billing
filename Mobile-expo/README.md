# Mobile - KGF Billing (Expo)

## Quick start (dev)
1. Install Node.js & Expo CLI on a computer (or use Expo Snack for quick tests).
2. In `mobile-expo` folder run:
   ```bash
   npm install
   expo start
